/**
 * 缩进测试用例
 * 1. 单个节点无法缩进
 * 2. 两个节点缩进第一个，第二个节点不会变化
 * 3. 两个节点缩进第二个，然后回车可以继续增加节点
 * 4. 可以带着子节点一起缩进
 * 5. 当已经有一个父节点的时候不能继续缩进
 */

describe('Should indent right', () => {
  it('Should not indent when there is only one node', () => {

  })
});